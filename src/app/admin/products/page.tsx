import AdminPlaceholder from "../_placeholder";

export default function Page() {
  return (
    <AdminPlaceholder
      title="Catalog"
      body="Create, duplicate, archive, variants, and Storage uploads land in Phase 4. Seed data is in supabase/seed.sql."
    />
  );
}
