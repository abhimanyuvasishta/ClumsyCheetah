import AdminPlaceholder from "../_placeholder";

export default function Page() {
  return (
    <AdminPlaceholder
      title="Orders"
      body="Amazon-style order table, filters, detail, and status history land in Phase 7. Schema and RLS are already in place."
    />
  );
}
